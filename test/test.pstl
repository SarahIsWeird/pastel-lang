extern print_n(value: Int32);

func myFunction(a: Int32, b: Int32): Int32 {
    // Do some magic
    return a + b;
}

func main() {
    var test: Int32 = myFunction(400, 20);

    print_n(test);

    test = 0;
    while (test < 10) {
        test = test + 1;
        print_n(test);
    }
}
